name: Keep Bot Alive
on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Bot
        run: |
          curl -s "https://miniapp-maps-aimodel.onrender.com" > /dev/null
